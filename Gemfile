# frozen_string_literal: true
source "https://rubygems.org"

# 锁定 GitHub Pages 官方依赖版本
gem "github-pages", group: :jekyll_plugins

# 本地预览所需（Windows 下建议安装）
gem "wdm", ">= 0.1.0", platforms: [:mswin, :mingw, :x64_mingw]
gem "webrick", "~> 1.8"       # Ruby 3.x 需要
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]  # Windows 时区数据
